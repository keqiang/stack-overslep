{
  "name": "example-02-http-driver",
  "private": true,
  "type": "module",
  "description": "Fix: stateless Neon HTTP driver lets the compute scale to zero.",
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "pg": "^8.13.1"
  }
}
